import Login from './login/page'; // Importando o componente Login

const Page = () => {
  return (
    <div>
      <Login /> {/* Renderizando o componente Login */}
    </div>
  );
};

export default Page;
